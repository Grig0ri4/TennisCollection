<Window Icon="/icon.ico"
        x:Class="TennisCatalog.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        xmlns:local="clr-namespace:TennisCatalog"
        mc:Ignorable="d"
        Title="Теннисная коллекция"
        Height="650" Width="950"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Background="White">
    <Window.Resources>
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="border" Background="#E3F2FD" CornerRadius="14,14,0,0" Padding="22,12" Margin="6,0">
                            <ContentPresenter ContentSource="Header" TextBlock.FontWeight="SemiBold"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsSelected" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="border" Property="Background" Value="#BBDEFB"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </MultiTrigger>

                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E90FF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid Margin="20">
            <TabControl>
                <TabItem Header="ФИНАЛЫ">
                    <Frame x:Name="FinalsFrame" NavigationUIVisibility="Hidden"/>
                </TabItem>
                <TabItem Header="ИГРОКИ">
                    <Frame x:Name="PlayersFrame" NavigationUIVisibility="Hidden"/>
                </TabItem>
                <TabItem Header="АНАЛИТИКА">
                    <Frame x:Name="StatsFrame" NavigationUIVisibility="Hidden"/>
                </TabItem>
                <TabItem Header="ЭНЦИКЛОПЕДИЯ">
                    <Frame x:Name="EncyclopediaFrame" NavigationUIVisibility="Hidden"/>
                </TabItem>
                <TabItem Header="ПРОФИЛЬ">
                    <Frame x:Name="UserFrame" NavigationUIVisibility="Hidden"/>
                </TabItem>
            </TabControl>

            <Button x:Name="AdminPanelButton" Content="Админ-панель" Width="130" Height="36" 
                Background="#FF4500" Foreground="White" HorizontalAlignment="Right" 
                VerticalAlignment="Top" Margin="0,3,10,0" Style="{StaticResource RoundedButton}" 
                Click="AdminPanelButton_Click" Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Window>