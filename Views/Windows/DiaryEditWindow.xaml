<Window x:Class="TennisCatalog.DiaryEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактировать заметку" 
        Width="380" 
        SizeToContent="Height" 
        WindowStartupLocation="CenterOwner" 
        ResizeMode="NoResize" 
        Background="#F4F7F9">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <StackPanel>
            <TextBlock Text="Ваша оценка (1-10):" FontWeight="Bold" FontSize="14" Foreground="#1E3A8A" Margin="0,0,0,10"/>

            <Slider x:Name="RatingSlider" 
                    Minimum="1" Maximum="10" 
                    TickFrequency="1" IsSnapToTickEnabled="True" SmallChange="1"/>

            <TextBlock Text="{Binding ElementName=RatingSlider, Path=Value}" 
                       HorizontalAlignment="Center" FontSize="26" Foreground="#3B82F6" FontWeight="Black" Margin="0,5,0,15"/>

            <TextBlock Text="Заметки о матче:" FontWeight="Bold" FontSize="14" Foreground="#1E3A8A" Margin="0,0,0,10"/>

            <TextBox x:Name="NotesBox" Height="100" TextWrapping="Wrap" AcceptsReturn="True" Padding="10" FontSize="14" BorderBrush="#CBD5E1" BorderThickness="1"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,0">
                <Button Content="СОХРАНИТЬ" Click="Save_Click" Width="120" Background="#10B981" Foreground="White" Margin="0,0,10,0"/>
                <Button Content="ОТМЕНА" IsCancel="True" Width="100" Background="#64748B" Foreground="White"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>