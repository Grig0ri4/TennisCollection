<Page x:Class="TennisCatalog.PlayersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Игроки" Background="Transparent">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="White" CornerRadius="12" Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#000000" Opacity="0.05"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="КАТАЛОГ ИГРОКОВ" FontSize="16" FontWeight="Black" Foreground="#0F172A" Margin="0,0,0,15"/>

                <StackPanel Grid.Row="1">
                    <ComboBox x:Name="TourComboBox" Margin="0,0,0,10" Height="38" Padding="10,5" SelectionChanged="TourComboBox_SelectionChanged"/>
                    <TextBox x:Name="SearchTextBox" Margin="0,0,0,15" Height="38" Padding="10,0" VerticalContentAlignment="Center" Background="#F8FAFC" BorderBrush="#E2E8F0" TextChanged="SearchTextBox_TextChanged"/>
                </StackPanel>

                <ListBox x:Name="PlayersList" Grid.Row="2" BorderThickness="0" SelectionChanged="PlayersList_SelectionChanged" DisplayMemberPath="full_name" FontSize="14" Margin="0,0,0,15">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="Margin" Value="0,2"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#DBEAFE"/>
                                    <Setter Property="Foreground" Value="#1E3A8A"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <StackPanel Grid.Row="3">
                    <Button Content="СРАВНИТЬ ИГРОКОВ" Click="OpenCompareWindow_Click" Height="40" Background="#1E3A8A" Foreground="White" FontWeight="Bold" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button Content="ЭКСПОРТ В EXCEL" Click="ExportButton_Click" Height="35" Margin="0,10,0,0" Background="#38BDF8" Foreground="White" FontWeight="Bold" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Column="2" Background="White" CornerRadius="12" Padding="30">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#000000" Opacity="0.05"/>
            </Border.Effect>
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel x:Name="StatsPanel" Visibility="Collapsed">

                    <!-- ИСПРАВЛЕНИЕ: Добавлен TextWrapping="Wrap" для длинных имен (например, с Германией) -->
                    <TextBlock x:Name="PlayerNameText" FontSize="34" FontWeight="Black" Foreground="#0F172A" Margin="0,0,0,5" TextWrapping="Wrap" LineHeight="40"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                        <TextBlock Text="Всего титулов: " FontSize="18" Foreground="#64748B"/>
                        <TextBlock x:Name="TotalTournamentsText" FontSize="20" FontWeight="Bold" Foreground="#2563EB"/>
                    </StackPanel>

                    <ItemsControl x:Name="TournamentStats">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="2" Margin="-5"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="20" Margin="5">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Rectangle Width="6" Height="40" Fill="#2563EB" RadiusX="3" RadiusY="3" Margin="0,0,15,0"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding TypeName}" FontSize="13" Foreground="#64748B" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding Count}" FontSize="26" FontWeight="Black" Foreground="#0F172A"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Button Content="ПОЛНАЯ АНАЛИТИКА" Click="OpenFullStats_Click" Height="45" Margin="0,30,0,0" Padding="35,0" Background="#0F172A" Foreground="White" FontWeight="Bold" HorizontalAlignment="Left" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>